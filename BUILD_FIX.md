# Shadow Awakening V1 — fixed build

This revision fixes the Kotlin compilation problems in the first ZIP:
- Removes the unnecessary AndroidX Activity dependency.
- Uses the platform Activity class.
- Fixes Color component access.
- Removes ambiguous pow() expressions.
- Keeps the same animation sequence.

Upload this ZIP as the replacement/fixed project in the builder.
